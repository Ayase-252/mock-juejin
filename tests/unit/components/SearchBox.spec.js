import { should } from 'chai'
import { shallowMount } from '@vue/test-utils'
import SearchBox from '../../src/components/SearchBox.vue'

describe('SearchBox', () => {
  it('should bind external data of parent with v-model', () => {
    
  })
})